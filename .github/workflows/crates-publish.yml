name: Publish greentic-setup (crate-only)
on:
  push:
    tags: ["setupcrates-v*"]
  workflow_dispatch: {}
permissions:
  contents: read
jobs:
  publish:
    uses: greenticai/.github/.github/workflows/crates-publish.yml@main
    with:
      crates: "greentic-setup"
    secrets: inherit
