# Auto-generated by sync-dev-publish.sh — do not edit manually.
# Source: REPO_MANIFEST.toml in greenticai/.github
name: Dev Publish

on:
  push:
    branches: [develop]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  dev-publish:
    uses: greenticai/.github/.github/workflows/dev-publish.yml@main
    with:
      crates: "greentic-setup"
      binary-crates: "greentic-setup"
      dual-role-binary-crates: "greentic-setup"
    secrets: inherit

  dev-binaries:
    needs: dev-publish
    strategy:
      fail-fast: false
      matrix:
        package: [greentic-setup]
    uses: greenticai/.github/.github/workflows/dev-release-binaries.yml@main
    with:
      package: ${{ matrix.package }}
      version: ${{ needs.dev-publish.outputs.version }}
